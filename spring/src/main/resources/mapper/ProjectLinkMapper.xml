<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="yyj.project.twinspring.dao.ProjectLinkDAO">

    <!--
        DDL (초기 1회 실행):

        CREATE TABLE IF NOT EXISTS project_link (
            link_id            VARCHAR(64) PRIMARY KEY,
            wbs_project_id     VARCHAR(64) NOT NULL REFERENCES wbs_project(project_id) ON DELETE CASCADE,
            linked_type        VARCHAR(32) NOT NULL,   -- BIM | SAFE | SIMULATION
            linked_project_id  VARCHAR(64) NOT NULL,
            note               TEXT,
            created_at         TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
            UNIQUE (wbs_project_id, linked_type, linked_project_id)
        );
        CREATE INDEX IF NOT EXISTS idx_project_link_wbs  ON project_link(wbs_project_id);
        CREATE INDEX IF NOT EXISTS idx_project_link_linked ON project_link(linked_type, linked_project_id);
    -->

    <!-- WBS 프로젝트에 연결된 모든 링크 (각 타입별 JOIN으로 이름/위치 채움) -->
    <select id="getLinksByWbsProject" parameterType="string" resultType="map">
        SELECT
            pl.link_id            AS "linkId",
            pl.wbs_project_id     AS "wbsProjectId",
            pl.linked_type        AS "linkedType",
            pl.linked_project_id  AS "linkedProjectId",
            pl.note               AS "note",
            TO_CHAR(pl.created_at, 'YYYY-MM-DD HH24:MI:SS') AS "createdAt",
            COALESCE(
                bp.project_name,
                sp.project_name,
                sim.project_name
            ) AS "linkedProjectName",
            COALESCE(
                bp.structure_type,
                sp.location,
                sim.project_name
            ) AS "linkedSubInfo",
            CASE pl.linked_type
                WHEN 'SAFE' THEN sp.location
                ELSE NULL
            END AS "linkedLocation",
            CASE pl.linked_type
                WHEN 'BIM'        THEN bp.structure_type
                WHEN 'SAFE'       THEN sp.status
                WHEN 'SIMULATION' THEN 'active'
                ELSE NULL
            END AS "linkedStatus"
        FROM project_link pl
        LEFT JOIN bim_project   bp  ON pl.linked_type = 'BIM'        AND pl.linked_project_id = bp.project_id
        LEFT JOIN safe_project  sp  ON pl.linked_type = 'SAFE'       AND pl.linked_project_id = sp.project_id
        LEFT JOIN simulation_project sim ON pl.linked_type = 'SIMULATION' AND pl.linked_project_id = sim.project_id
        WHERE pl.wbs_project_id = #{wbsProjectId}
        ORDER BY pl.linked_type ASC, pl.created_at ASC
    </select>

    <select id="getLinksByWbsProjectAndType" resultType="map">
        SELECT
            pl.link_id            AS "linkId",
            pl.wbs_project_id     AS "wbsProjectId",
            pl.linked_type        AS "linkedType",
            pl.linked_project_id  AS "linkedProjectId",
            pl.note               AS "note",
            TO_CHAR(pl.created_at, 'YYYY-MM-DD HH24:MI:SS') AS "createdAt"
        FROM project_link pl
        WHERE pl.wbs_project_id = #{wbsProjectId}
          AND pl.linked_type    = #{linkedType}
        ORDER BY pl.created_at ASC
    </select>

    <!-- 역방향: 하위 프로젝트가 어떤 WBS에 연결됐는지 -->
    <select id="getWbsByLinkedProject" resultType="map">
        SELECT
            pl.link_id            AS "linkId",
            pl.wbs_project_id     AS "wbsProjectId",
            wp.project_name       AS "wbsProjectName",
            pl.linked_type        AS "linkedType",
            pl.linked_project_id  AS "linkedProjectId",
            pl.note               AS "note",
            TO_CHAR(pl.created_at, 'YYYY-MM-DD HH24:MI:SS') AS "createdAt"
        FROM project_link pl
        LEFT JOIN wbs_project wp ON wp.project_id = pl.wbs_project_id
        WHERE pl.linked_type      = #{linkedType}
          AND pl.linked_project_id = #{linkedProjectId}
        ORDER BY pl.created_at ASC
    </select>

    <insert id="insertLink" parameterType="map">
        INSERT INTO project_link (link_id, wbs_project_id, linked_type, linked_project_id, note)
        VALUES (#{linkId}, #{wbsProjectId}, #{linkedType}, #{linkedProjectId}, #{note})
    </insert>

    <delete id="deleteLink" parameterType="string">
        DELETE FROM project_link WHERE link_id = #{linkId}
    </delete>

    <delete id="deleteLinksByWbsProject" parameterType="string">
        DELETE FROM project_link WHERE wbs_project_id = #{wbsProjectId}
    </delete>

    <select id="countLink" resultType="int">
        SELECT COUNT(*) FROM project_link
        WHERE wbs_project_id    = #{wbsProjectId}
          AND linked_type       = #{linkedType}
          AND linked_project_id = #{linkedProjectId}
    </select>

</mapper>
