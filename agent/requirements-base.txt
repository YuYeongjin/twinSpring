# ══════════════════════════════════════════════════════════════
#  Layer 1 — Heavy ML / Embedding dependencies
#  Install FIRST so Docker can cache this layer independently.
#
#  These packages pull PyTorch (~1.5 GB) and related libs.
#  On CPU-only servers add:
#    --extra-index-url https://download.pytorch.org/whl/cpu
#  to pip install to avoid downloading the CUDA build.
#
#  Install:  pip install -r requirements-base.txt
# ══════════════════════════════════════════════════════════════

# Sentence embeddings (intfloat/multilingual-e5-small etc.)
# Pulls torch, transformers, tokenizers
sentence-transformers>=3.0.0

# LangChain HuggingFace integration
langchain-huggingface>=0.1.0
