# ──────────────────────────────────────────────────────────────────────
# [무거운 레이어] PyTorch를 끌어오는 패키지 — 거의 변경되지 않음
# sentence-transformers → torch (~1.5 GB), transformers 등
# chromadb → 대형 벡터 DB 의존성
# langchain-huggingface → sentence-transformers에 의존
# ──────────────────────────────────────────────────────────────────────
sentence-transformers>=3.0.0
chromadb>=0.5.0
langchain-huggingface>=0.1.0
